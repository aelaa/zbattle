%h1 Новости

= link_to 'Новая новость', new_admin_news_article_path

%table
  %thead
    %tr
      %th Имя
      %th
      %th
      %th

  %tbody
    - news.each do |article|
      %tr
        %td= article.name
        %td= link_to 'Show', admin_news_article_path(article)
        %td= link_to 'Edit', edit_admin_news_article_path(article)
        %td= link_to 'Destroy', admin_news_article_path(article), method: :delete, data: { confirm: 'Are you sure?' }

%br


